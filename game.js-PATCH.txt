=== PATCH for game.js ===

1. FIND this block (inside beginLineClear, around the level-up code):

    if (leveledUp) {
      Audio_.sfx.levelUp();
      Effects.toast(`LEVEL ${level}`, 'levelup');
    }

   REPLACE with:

    if (leveledUp) {
      Audio_.sfx.levelUp();
      Effects.toast(`LEVEL ${level}`, 'levelup');
      Audio_.setMusicLevel(level);   // ← ADD THIS LINE
    }


2. (Optional but recommended) Update music start calls so they pass the level:

   FIND in beginPlaying():
     if (saveData.settings.musicOn) Audio_.startMusic();

   REPLACE with:
     if (saveData.settings.musicOn) Audio_.startMusic(level);


   FIND in togglePause() (the resume branch):
     if (saveData.settings.musicOn) Audio_.startMusic();

   REPLACE with:
     if (saveData.settings.musicOn) Audio_.startMusic(level);
