=== Small HTML change needed in index.html ===

Inside <div class="start-scroll"> you currently have this order:

  1. hud-corner (BEST score)
  2. start-top          ← BLØKS title  (will be hidden by CSS)
  3. menu-selector-row  ← Theme / Block / Mode cards
  4. hero-stage         ← floating blocks + PLAY button
  5. pill-nav           ← Stats / Achievements / Settings

To make the bottom panel clean, wrap the two bottom rows together.

FIND this (near the end of the start-scroll section):

      </div>   <!-- end of hero-stage -->

      <nav class="pill-nav">
        ...
      </nav>
      <div class="pill-nav__indicator"></div>

REPLACE the opening of the pill-nav block with:

      </div>   <!-- end of hero-stage -->

      <!-- UNIFIED BOTTOM PANEL -->
      <div class="menu-bottom-panel">
        <!-- the existing menu-selector-row will be moved here by CSS order,
             but if you want the cleanest DOM, cut the whole
             <div class="menu-selector-row">...</div> block from higher up
             and paste it right here, before the <nav> -->

        <nav class="pill-nav">
          ... (leave the three buttons exactly as they are)
        </nav>
      </div>
      <!-- remove the old <div class="pill-nav__indicator"></div> -->

OPTIONAL (cleaner DOM):
  Cut the entire <div class="menu-selector-row" ...>...</div> block
  from its current position (right after start-top) and paste it
  as the first child inside the new <div class="menu-bottom-panel">.
  The CSS will still work either way because of the order / flex rules.

After the change the visual hierarchy on mobile becomes:

  ┌─────────────────────────────┐
  │  BEST score (top-left)      │
  │                             │
  │      (art background)       │
  │         [ PLAY ]            │
  │                             │
  │  ┌─────┬─────┬─────┐        │
  │  │THEME│BLOCK│MODE │        │  ← selectors now at bottom
  │  └─────┴─────┴─────┘        │
  │  ┌─────┬─────┬─────┐        │
  │  │STATS│ACHV │SETT │        │  ← same panel, one UI
  │  └─────┴─────┴─────┘        │
  └─────────────────────────────┘
